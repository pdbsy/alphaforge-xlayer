# AlphaForge M3 Closeout Implementation Plan

> **For agentic workers:** Execute task-by-task using the existing Macbeth01–05 assignments; no additional worker identities. The executing-plans workflow applies, subject to the user's instruction to continue all unblocked authorized work without another start confirmation.

**Goal:** Deliver a traceable common Pass/Vault integration candidate, independently verified where prerequisites permit, and publish a reviewable PR with explicit blockers.

**Architecture:** Preserve worker commits and source refs in the manager integration branch. Resolve shared metadata conflicts explicitly; keep Local and Testnet boundaries intact. Scope-dependent protocol choices remain user decisions.

**Tech Stack:** Node 24.21.0, npm 11.19.1, TypeScript, Fastify/SQLite, existing browser entrypoint and approved Solidity toolchain.

**Spec:** User AF-M3-CLOSEOUT instructions, SHA-256 `c5a6dfa8e4be082e2ee3da5313ef3a3e4c89ccde9418ef1ea8647c5eba3936a6`; execution record in `docs/management/tasks/AF-M3-CLOSEOUT.md`.

## Global Constraints

- Canonical repository: `pdbsy/quantpass-arbitrum-hackathon`.
- Task/branch: `AF-M3-CLOSEOUT` / `macbeth01/AF-M3-CLOSEOUT`.
- Fixed base: `7ecba357d5a19f387e86f578822af04a6261fed2`.
- Complete pre-runtime Pass/Vault user flow; strategy execution is explicitly deferred.
- No master updates, PR merges, rule removal, history rewrite, chain access, signatures or deployments.
- Preserve authors, QA versions, failures and generated C/R/S evidence. No shared writable dependencies/data.

## Task 1 — Delivery and decision record

Files: `docs/management/tasks/AF-M3-CLOSEOUT.md`, existing registry/bootstrap/current-status records.

- [x] Read user instruction, official refs, source ancestry and six QA reports at `2efed688353579160a5f03ed4fa1f4b52178ecdf`.
- [x] Confirm 04 contains fixed 03 and lacks 02; distinguish QA report SHA from tested source SHAs.
- [x] Resolve acceptance scope from new user instruction; request only remaining protocol/finality decisions.
- [ ] Register current closeout and workers' actual tasks without claiming milestone completion.
- [ ] Record each final delivery and unresolved decision in the existing task record.

## Task 2 — Restricted manager integration identity

Files: `tools/agent-integration-identity.mjs`, `tools/check-agent-identity.mjs`, `test/agent-integration-identity.test.mjs`, integration source manifest, existing package/test registries.

User approved `AF-M3-CLOSEOUT-ID-001`: permit only the designated branch/task, fixed base and pinned registered sources; validate original Agent/Task provenance and manager additions. Required CI remains unchanged.

- [ ] Add real Git-fixture tests for approved mixed provenance and rejection of wrong branch/base, missing source ancestry, invalid source identity, unregistered imported commits and invalid manager additions. Observe failure before implementation.
- [ ] Implement bounded manifest parsing and graph validation; read source-head manifest rather than mutable checkout content; keep ordinary worker/master paths unchanged.
- [ ] Run existing lifecycle/bypass suites plus new tests and obtain focused independent review from an existing reviewer or Macbeth05.

## Task 3 — Worker corrections and protocol prerequisites

Interfaces: 02 supplies actual owner-only Vault ABI/events/views and decisions; 03 consumes those exact artifacts; 04 consumes 03 normalized evidence at the real product entry; 05 verifies exact candidates.

- [ ] 02 supplies the smallest unresolved accounting/owner/precision decision request and continues unambiguous implementation and tests.
- [ ] 03 checks ancestry/composition and prepares ABI consumption without guessed calldata or policy constants.
- [ ] 04 rejects contradictory ready/failure evidence, preserves ambiguous submissions and tests the actual entrypoint/browser states.
- [ ] Record user answers before dependent code; do not mark mandatory Pass/Vault implementation deferred.

## Task 4 — Common candidate and metadata resolution

Files: worker source changes plus shared `package.json`, provenance, registry/bootstrap, current status and generated snapshots.

- [ ] Merge exact 04 source (already contains 03), 02 source, manager task registrations and published QA reports into the manager branch; preserve original commits.
- [ ] Resolve shared files by retaining both valid contributions and recording the final adaptation hashes, never broad ours/theirs selection.
- [ ] Update pinned sources after worker corrections; designate exact candidate SHA and included/excluded capabilities.
- [ ] Request 05 independent scope-appropriate retests on that exact candidate; no combined PASS inferred from old slice results.

## Task 5 — Evidence and actual PR

- [ ] Run exact approved toolchain engineering, contract and meaningful affected integration/browser checks; keep unavailable/unauthorized Testnet tasks NOT_RUN or BLOCKED_ON_AUTHORIZATION.
- [ ] Preserve source C, collect R, generate S, and validate the final head; source changes after QA require impact analysis and affected retests.
- [ ] Publish manager branch and PR targeting master; record PR15–19 inclusion/dependency relationships without closing or merging them.
- [ ] Verify remote SHA and actual hosted checks. Use Draft while required work or decisions remain; report PARTIAL_BLOCKED rather than completed.

## Plan review

The tasks cover delivery verification, decisions, owner-specific remediation, traceable integration, independent QA, engineering/contract/browser evidence, truthful documentation and actual PR publication. Protocol code steps are owned by the workers' existing plans and cannot be defined before unresolved user semantics; this plan does not invent those semantics. No new feature, oracle, economics parameter, runtime or chain-operation scope is introduced.
